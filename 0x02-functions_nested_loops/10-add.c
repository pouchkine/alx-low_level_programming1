#include "main.h"
/**
 * add - Add two integers and returns the result.
 * @a: first argument.
 * @b: second argument.
 *
 * Return: the sum of a and b - a+b.
 */
int add(int a, int b)
{
	return (a + b);
}
